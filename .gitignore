# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage

# Virtual envs
.venv/
venv/

# Build output (PDFs are generated from worksheets/ via `mise run build-all`)
dist/
build/
*.pdf

# CI per-site staging dirs (release root + draft subdir, assembled into dist/)
site-draft/
site-release/

# Local Claude Code settings (the badge-yaml skill IS committed)
.claude/settings.local.json

# Editor / OS cruft
*.swp
*~
.DS_Store
